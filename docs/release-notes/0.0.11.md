PROJECT_ICON becomes `folder-kanban`.

minAppVersion 1.8.7 -> 1.11.0, and 0.0.11 in versions.json. `registerBasesView()`
landed in 1.10.2, and 1.11.0 has been public since January, and would let
the six statuses render over a scope the user already has.
